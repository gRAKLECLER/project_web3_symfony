{% extends 'base.html.twig' %}

{% block body %}
    <style>
        .ad-container { background: red; margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    </style>

    <h1>All Ads</h1>

    <ul>
        {% for ad in ads %}
            <li>
                <a href="{{ path('app_ad', {id: ad.id}) }}">
                    {{ ad.title }} - {{ ad.description }} - {{ ad.price}}€
                </a>
            </li>
        {% endfor %}
    </ul>
{% endblock %}
